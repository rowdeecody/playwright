{
    "compilerOptions": {
      "baseUrl": ".", // This must be specified if "paths" is.
      "paths": {
        "@myhelper/*": ["packages/helpers/*"] // This mapping is relative to "baseUrl".
      }
    }
  }