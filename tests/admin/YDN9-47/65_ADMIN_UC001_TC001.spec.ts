import { test, expect } from '@playwright/test';

test('Verify display contract selection page', async ({ page }) => {

    console.log(page)
    // await page.goto('/mnt/certification/mst/mst_menu.php');
    // await page.getByRole('button', { name: 'YEASTデータ取込管理（特約店契約）' }).click();
    // await page.getByRole('link', { name: '登録フォーム（追加・更新）' }).click();
});

// test('Verify display contract selection page', async ({ page }) => {
// });